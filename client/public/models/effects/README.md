# Muzzle Flash Textures

Place 2–3 PNG muzzle flash textures here. Transparent background recommended.

Expected filenames (configurable in `client/src/config/index.ts`):
- `muzzle1.png`
- `muzzle2.png`
- `muzzle3.png`

If load fails, a procedural fallback texture is used.
